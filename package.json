{
  "name": "glip-zendesk-bot",
  "version": "0.1.0",
  "private": true,
  "license": "All right reserved",
  "scripts": {
    "test": "node index.js",
    "upgrade": "yarn-upgrade-all && yarn remove glip.socket.io && cp -r ../glip.socket.io node_modules/"
  },
  "devDependencies": {
    "standard": "^11.0.1",
    "yarn-upgrade-all": "^0.3.0"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^5.0.1",
    "ramda": "^0.25.0",
    "striptags": "^3.1.1"
  }
}
