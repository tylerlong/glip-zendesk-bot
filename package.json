{
  "name": "glip-zendesk-bot",
  "version": "0.1.0",
  "private": true,
  "license": "All right reserved",
  "scripts": {
    "test": "node index.js",
    "upgrade": "yarn-upgrade-all && cp -r ../glip.socket.io node_modules/"
  },
  "devDependencies": {
    "eslint-plugin-html": "^4.0.3",
    "standard": "^11.0.1",
    "yarn-upgrade-all": "^0.3.0"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^6.0.0",
    "ramda": "^0.25.0",
    "striptags": "^3.1.1"
  }
}
